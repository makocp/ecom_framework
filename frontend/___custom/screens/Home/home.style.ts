import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textStyle: {
        fontFamily: 'bold',
        fontSize: 20
    },
    appBarWrapper: {
        marginHorizontal: 22,
        marginTop: 10
    }
});

export default styles;